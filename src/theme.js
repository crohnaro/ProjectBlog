import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#bc81e9",
    },
    secondary: {
      main: "#FFF6FF",
    },
    background: {
      default: "#201925",
      white: "#FFF6FF",
    },
  },
});

export default theme;
