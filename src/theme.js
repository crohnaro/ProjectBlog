import { createTheme } from "@mui/material";


const theme = createTheme({
    palette: {
        primary: {
            main: '#A835FF',
        },
        secondary: {
            main: "#A187B0"
        },
        background: {
            default: "#3B2A45",
            white: "#FFF6FF"
        }
    },
    
})

export default theme